<div class="row">
  <section class="large-9 columns">
    <h1><%= @ad.title %></h1>
    <p><%= @ad.description %></p>
    <dd>
      <dl><%= @ad.car.make.name %>, <%= @ad.car.model.name %></dl>
      <dl>Engine: <%= @ad.car.engine %></dl>
      <dl>Power: <%= @ad.car.hp %></dl>
      <dl>Year: <%= @ad.car.age %></dl>
      <dl>Fuel Type: <%= @ad.car.fuel_type %></dl>
      <dl>Style: <%= @ad.car.style %></dl>
      <dl>Color: <%= @ad.car.color %></dl>
    </dd>
    <div class="row">
      <section class="panel">
        <ul class="clearing-thumbs" data-clearing>
          <% @ad.images.each do |image| %>
            <li><a href="<%= image.carphoto_url %>"><img src="<%= image.carphoto.thumb.url %>"></a></li>
          <% end %>
        </ul>
      </section>
    </div>
  </section>
  <div class="large-3 columns right">
    <div class="panel ad-user-info">
    <h3>Seller Info:</h3>
    <%= image_tag gravatar_picture_url(@ad.user, 120), :class => "ad-user-picture" %>
    <dl>
      <dd>Nick: <%= link_to @ad.user.nick, user_profiles_path(@ad.user) %></dd>
      <dd>Contact</dd>
    </dl>
  </div>
  </div>
</div>