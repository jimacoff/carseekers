<%= form_for @profile, :url => user_profile_path(current_user, @profile) do |f| %>
    <div><%= f.label :bio %><br />
    <%= f.text_area :bio %></div>
    <div><%= f.submit "Update", :class => "button [success small radius round]" %></div>
<% end %>